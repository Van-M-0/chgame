package main


func testCommunicator() {
/*
	type Hello struct {
		A 		int
		Str 	string
		Slic 	[]int
	}

	var buf bytes.Buffer
	fmt.Fprint(&buf, &Hello{
		A:100,
		Slic: []int{123, 3},
		Str: "hello world",
	})

	mylog.Debug(buf.Bytes(), string(buf.Bytes()))
	mylog.Debug("test comunicaor")
	c := communicator.NewCommunicator()
	c.JoinChanel("hello", false, defines.WaitChannelInfinite, func(data []byte) {
		mylog.Debug("hello channel ", data, string(data))
	})
	c.JoinChanel("h*", true, defines.WaitChannelInfinite, func(data []byte) {
		mylog.Debug("hello channel ", string(data))
	})

	time.Sleep(10*time.Millisecond)
	c.Notify("hello", "aaa", "bbb")
*/
}
